class_name Constants
enum CharacterFraction { WARDEN, COLLECTOR, SUCKER }
enum CharacterType { ROCK, PAPER, SCISSORS }
enum PickupType { DUPLICATE }
enum Mode { MOVE, ATTACK, MERGE }
enum Level { TEST, SKIN, BLOOD, BRAIN, NEURAL, STARTUP }
